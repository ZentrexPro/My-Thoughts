// Particle Backgroundconst canvas = document.getElementById('bg');const ctx = canvas.getContext('2d');let particles = [];const particleCount = 120;function initParticles(){particles=[];for(let i=0;i<particleCount;i++){particles.push({x:Math.random()*canvas.width,y:Math.random()*canvas.height,r:Math.random()*2+1,dx:(Math.random()-0.5)*0.6,dy:(Math.random()-0.5)*0.6,color:`hsl(${Math.random()*360},80%,60%)`});}} function resizeCanvas(){canvas.width=window.innerWidth;canvas.height=window.innerHeight;initParticles();}window.addEventListener('resize',resizeCanvas);resizeCanvas();function animate(){ctx.clearRect(0,0,canvas.width,canvas.height);particles.forEach(p=>{ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fillStyle=p.color;ctx.fill();p.x+=p.dx;p.y+=p.dy;if(p.x<0)p.x=canvas.width;if(p.x>canvas.width)p.x=0;if(p.y<0)p.y=canvas.height;if(p.y>canvas.height)p.y=0;});requestAnimationFrame(animate);}animate();// Typewriter Quoteconst text="You deserve like a gold 🪙 <br>and<br> I deserve like a diamond 💎";const target=document.getElementById('quote-text');let index=0;function typeWriter(){if(index<text.length){let char=text.charAt(index);if(char!=='<'){const span=document.createElement('span');span.innerHTML=char;target.appendChild(span);index++;setTimeout(typeWriter,70);}else{if(text.substring(index,index+4)==='<br>'){target.innerHTML+='<br>';index+=4;}else{index++;}typeWriter();}}else{const cursor=document.createElement('span');cursor.classList.add('cursor');target.appendChild(cursor);}}typeWriter();// Message Systemconst sendBtn=document.getElementById('send-btn');const input=document.getElementById('message-input');const messagesList=document.getElementById('messages-list');function loadMessages(){messagesList.innerHTML='';const messages=JSON.parse(localStorage.getItem('messages')||'[]');messages.forEach((msg,i)=>{const div=document.createElement('div');div.classList.add('message-item');div.innerHTML=`<strong>${msg.name}</strong>: ${msg.text} <button class="delete-btn" data-index="${i}">X</button>`;messagesList.appendChild(div);});}sendBtn.addEventListener('click',e=>{e.preventDefault();const value=input.value.trim();if(!value)return;const messages=JSON.parse(localStorage.getItem('messages')||'[]');messages.push({name:'Anonymous',text:value});localStorage.setItem('messages',JSON.stringify(messages));input.value='';loadMessages();});messagesList.addEventListener('click',e=>{if(e.target.classList.contains('delete-btn')){const index=e.target.dataset.index;let messages=JSON.parse(localStorage.getItem('messages')||